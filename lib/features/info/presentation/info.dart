import 'package:taro_leaf_blight/packages/global_packages.dart';

class DetectionInfoScreen extends ConsumerWidget {
  const DetectionInfoScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Placeholder();
  }
}
